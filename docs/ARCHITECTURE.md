# Architecture

> This document will be expanded in Phase 8 — Docs.

## Overview

Distributed Job Scheduler — a background job scheduling platform with:
- FastAPI backend with async SQLAlchemy
- PostgreSQL database
- Custom polling worker service
- React (Vite) frontend dashboard

See `AGENTS.md` for the full stack and repo layout.
